/*
#include <iostream>
using namespace std;

int main()
{	
	// Declare an array of size 5
	int numbers[5];
	// a variable to store the total sum of the integers in the array
	int total = 0;
	// for loop for taking input as elements of the array
	for (int i = 0; i < 5; i++) {
		cin >> numbers[i];
	}
	// add the number entered to the total sum
	for (int i = 0; i < 5; i++) {
		total += numbers[i];
	}
	// display total on console
	cout << "Total sum of the numbers is " << total;

	return 0;
}
*/